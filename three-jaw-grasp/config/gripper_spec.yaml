gripper:
  name: "UFO Catcher Three-Jaw Gripper"

  # ─────────────────────────────────────────────────────────────────
  # !! 아래 값들은 임시 기본값(placeholder)입니다 !!
  # 3D 프린터로 제작한 실제 집게를 측정한 후 반드시 업데이트하세요.
  #
  # 측정 방법:
  #   min_width    : 집게가 완전히 오므렸을 때 3발 끝단 사이의 최소 직경 (m)
  #   max_width    : 집게가 최대로 벌어졌을 때 3발 끝단 사이의 직경 (m)
  #   ideal_width  : 가장 안정적으로 파지되는 것으로 실험 확인된 폭 (m)
  #                  → 처음에는 (min + max) / 2 로 설정하고 실험 후 조정
  #   collision_threshold_z : 카메라 또는 기준점에서 바닥까지 거리 중
  #                           집게가 충돌할 위험이 있는 임계 높이 (m)
  # ─────────────────────────────────────────────────────────────────
  min_width: 0.01               # TODO: 실제 측정값으로 교체
  max_width: 0.10               # TODO: 실제 측정값으로 교체
  ideal_width: 0.05             # TODO: 실험 후 조정
  collision_threshold_z: 0.02   # TODO: 카메라 설치 높이 기반으로 재설정

evaluation:
  rule_weights:
    # ─────────────────────────────────────────────────────────────
    # 가중치 설정 기준
    #
    # width     : 집게가 물체를 실제로 잡을 수 있는지 (범위 밖이면 0점)
    # score     : 탐지 모델의 신뢰도 반영
    # height    : 너무 낮으면 바닥 충돌 위험 → 높이 안전 마진 확보
    # stability : 집게 접근 각도와 3발 배치의 정렬 도 (60° 주기)
    # symmetry  : 파지 각도와 120° 3발 대칭 구조의 정렬도
    #
    # !! 주의 !!
    # 3발이 하드웨어적으로 항상 120° 균등 배치인 경우,
    # 발 사이 각도 자체는 평가할 필요가 없습니다.
    # symmetry와 stability는 "집게를 몇 도 방향으로 회전해 접근할지"
    # 즉 집게 자체의 Z축 회전 방향과 물체 형상의 정렬 문제입니다.
    #
    # 인형뽑기 기계처럼 물체가 주로 원통/구형일 경우:
    #   → symmetry: 0.00  (회전 방향 무관)
    #   → stability 가중치는 낮추고 width/height/score 높이기 권장
    #
    # 현재 값은 범용 초기값입니다. 실험 후 조정하세요.
    # ─────────────────────────────────────────────────────────────
    width:     0.35   # 파지 폭 적합도 (높임 — 가장 중요한 물리 제약)
    score:     0.30   # 탐지 신뢰도
    height:    0.25   # 높이 안전 마진
    stability: 0.10   # 집게 접근 각도 정렬 (원통형 물체 많으면 낮춰도 됨)
    symmetry:  0.00   # 3발이 항상 120° 고정이므로 기본 0으로 설정
